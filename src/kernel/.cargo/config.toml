# .cargo/config.toml — CosinusOS Kernel

[build]
target = "x86_64-unknown-none"

[target.x86_64-unknown-none]
rustflags = [
    "-C", "relocation-model=static",
    "-C", "panic=abort",
    "-C", "force-frame-pointers=yes",
]