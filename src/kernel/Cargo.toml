# src/kernel/Cargo.toml
[package]
name = "kernel"
version = "0.1.0"
edition = "2021"  # ← Tymczasowo 2021, by uniknąć problemów z unsafe

[dependencies]


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"