[package]
name    = "libcosinus"
version = "0.1.0"
edition = "2021"

[lib]
name       = "libcosinus"
crate-type = ["rlib","staticlib"]

[profile.release]
opt-level = "s"
lto       = true
panic     = "abort"